//In this lesson FCC want you to access array data with indexes!

/*
We can access the data inside arrays using indexes.

Array indexes are written in the same bracket notation that strings use, except that instead of 
specifying a character, they are specifying an entry in the array. 

Like strings, arrays use zero-based indexing, so the first element in an array is element 0.

Task:

Create a variable called myData and set it to equal the first value of myArray using bracket notation.
*/

// Example
var ourArray = [50, 60, 70];
var ourData = ourArray[0]; // equals 50

// Setup
var myArray = [50, 60, 70];
var myData = myArray[0];
// Only change code below this line.
